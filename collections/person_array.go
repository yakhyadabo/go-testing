package collections

// Arrays in Go are fixed-size, homogenous data structures that store elements of the same type.


// var myArray [5]int // declares an array of integers with a size of 5
// myArray := [5]int{1, 2, 3, 4, 5} // initializes an array with values:

// Looping

/*
for i, value := range myArray {
	fmt.Printf("Element at index %d is %d\n", i, value)
} */
